# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?

# Credentials and sensitive files — never commit these
*.key
*.pem
*.crt
*.p12
*.pfx
.env
*.token
*password*
*secret*
*credential*

# Splunk — never commit these
splunk-enttrial.lic
*.lic
inputs.conf.bak
outputs.conf.bak

# Editor artifacts
.vscode/
.idea/
*.swp
*.swo
*~

# Python (for any scripted inputs committed later)
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Logs that aren't session logs — raw system log dumps, test files
*.log
